<div class="container w-50 mt-3">
  <h1>Add a movie to "<%= @list.name %>"</h1>
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment, placeholder: 'Écrivez votre commentaire !' %>
    <%= f.button :submit, class: "btn btn-success mt-3" %>
  <% end %>
</div>
